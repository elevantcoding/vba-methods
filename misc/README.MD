# miscellaneous vba

1. InterpolateColor - steps through color hues from white to desired RGB, includes form module code example of use (used for splash screen)
2. CompareStrings - compare two string values and return a % difference to help avoid near-duplicates and for inference
